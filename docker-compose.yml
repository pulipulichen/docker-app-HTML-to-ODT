version: "3"
services:
  app:
    build: .
    tty: true
    command: pandoc --standalone --embed-resources=true /app/input2.html -o /app/output.odt
    volumes: 
      - ./app:/app/
      - ./input:/input/