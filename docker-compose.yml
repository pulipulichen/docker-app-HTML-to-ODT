version: "3"
services:
  app:
    build: .
    tty: true
    command: pandoc --standalone --embed-resources=true /input/a.html -o /input/output.odt
    volumes: 
      - ./app:/app/
      - ./input:/input/